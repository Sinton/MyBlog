---
import "../styles/global.css";
import BackToTop from "../components/BackToTop.astro";
import SmoothScroll from "../components/SmoothScroll.astro";
import {
  defaultThemeConfig,
  type NewspaperThemeConfig,
} from "../lib/theme-config";
import mastheadSubsetLateUrl from "@fontsource/zhi-mang-xing/files/zhi-mang-xing-114-400-normal.woff2?url";
import mastheadSubsetMeBaoUrl from "@fontsource/zhi-mang-xing/files/zhi-mang-xing-118-400-normal.woff2?url";
import mastheadSubsetDeUrl from "@fontsource/zhi-mang-xing/files/zhi-mang-xing-119-400-normal.woff2?url";
import jetbrainsMonoUrl from "@fontsource/jetbrains-mono/files/jetbrains-mono-latin-400-normal.woff2?url";

interface Props {
  title?: string;
  description?: string;
  config?: NewspaperThemeConfig;
}

const { config = defaultThemeConfig } = Astro.props;
const title = Astro.props.title ?? config.siteTitle;
const description = Astro.props.description ?? config.description;
const pageTitle =
  title === config.siteTitle ? title : `${title} | ${config.siteTitle}`;

const fontFaceCss = `
@font-face {
  font-family: "Newspaper Masthead Calligraphy";
  font-style: normal;
  font-display: block;
  font-weight: 400;
  src: url("${mastheadSubsetLateUrl}") format("woff2");
  unicode-range: U+665A;
}

@font-face {
  font-family: "Newspaper Masthead Calligraphy";
  font-style: normal;
  font-display: block;
  font-weight: 400;
  src: url("${mastheadSubsetMeBaoUrl}") format("woff2");
  unicode-range: U+6211,U+62A5;
}

@font-face {
  font-family: "Newspaper Masthead Calligraphy";
  font-style: normal;
  font-display: block;
  font-weight: 400;
  src: url("${mastheadSubsetDeUrl}") format("woff2");
  unicode-range: U+7684;
}

@font-face {
  font-family: "JetBrains Mono";
  font-style: normal;
  font-display: swap;
  font-weight: 400;
  src: url("${jetbrainsMonoUrl}") format("woff2");
}
`;
---

<!doctype html>
<html lang={config.lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="preload" href={mastheadSubsetLateUrl} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={mastheadSubsetMeBaoUrl} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={mastheadSubsetDeUrl} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={jetbrainsMonoUrl} as="font" type="font/woff2" crossorigin />
    <style is:inline set:html={fontFaceCss}></style>
    <title>{pageTitle}</title>
  </head>
  <body>
    <div class="paper-shell">
      <slot />
    </div>
    {config.enableSmoothScroll && <SmoothScroll />}
    <BackToTop label={config.backToTopLabel} />
  </body>
</html>
